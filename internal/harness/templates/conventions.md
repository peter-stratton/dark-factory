# Conventions

<!-- Guidance: This document captures project-wide coding conventions. Each
     section should be specific enough that a new contributor can follow it
     without asking questions. Replace the placeholder text in each section
     with your actual conventions. -->

## Error Handling

<!-- Guidance: Describe how errors are created, wrapped, and surfaced. For
     example: always wrap with context, use sentinel errors vs typed errors,
     never swallow errors silently, how to handle recoverable vs fatal
     errors. -->

## Logging

<!-- Guidance: Describe the logging library, log levels in use, required
     structured fields, and what events should and should not be logged.
     For example: use slog with structured fields, always include a
     request ID, do not log sensitive data. -->

## Testing

<!-- Guidance: Describe the testing strategy — unit vs integration vs
     end-to-end, test file naming, test helper conventions, how to stub
     external dependencies, and any required coverage thresholds. -->

## Naming

<!-- Guidance: Describe naming conventions for packages, files, types,
     functions, and variables. For example: package names are singular
     nouns, exported types use PascalCase, unexported variables use
     camelCase, test files are named foo_test.go. -->
